# เพิ่มผู้ใช้งานโดยไม่ต้อง deploy รายชื่อ

ตั้งค่าฝั่ง server ของโปรเจกต์ ScanQr_eiei บน Vercel แล้ว deploy โค้ดครั้งแรก:

- `MONGODB_URI`: MongoDB connection string ของฐานข้อมูลที่จะเก็บรายชื่อ (ห้ามใช้ชื่อที่ขึ้นต้น VITE_)
- `MONGODB_DATABASE`: ชื่อฐานข้อมูล ค่าเริ่มต้น attendanceDB
- `QR_ADMIN_KEY`: รหัสลับผู้ดูแลอย่างน้อย 32 ตัวอักษร เก็บเฉพาะ server และให้เฉพาะผู้ดูแล
- คง `QR_SECRET` เดิมไว้สำหรับเซ็น QR

เปิด `/admin/students` กรอกรหัสผู้ดูแล รหัสผู้ใช้งาน และชื่อ–นามสกุล รายชื่อใหม่ถูกเก็บใน collection `qr_students` โดยใช้รหัสผู้ใช้งานเป็น `_id` เพื่อป้องกันรหัสซ้ำ ไม่เขียนทับคนเดิม

ผู้ใช้งาน Login บนหน้าเดิมได้ทันทีหลังบันทึก และสร้าง QR ไปลงทะเบียนใบหน้าในเว็บเช็คชื่อ ไม่มีการบันทึกเช็คชื่อหรือใบหน้าในขั้นตอนเพิ่มรายชื่อ

รายชื่อเก่าใน students.csv และแหล่งรายชื่อเดิมยังใช้ได้ หากตั้ง MongoDB แล้วแต่เชื่อมต่อไม่ได้ API จะตอบ 503 เพื่อไม่รายงานผิดว่าไม่พบรหัส

ข้อมูลใบหน้าของ Face API และ CCTV ยังเป็นคนละฐานกับรายชื่อนี้ ระบบรายงานเช็คชื่อที่ใช้ฐานอื่นไม่ได้ถูกย้ายหรือซิงก์โดยการเปลี่ยนแปลงนี้

ทดสอบโดยไม่แตะฐานจริง: `python -m unittest test_student_admin.py`
